# Third-party notices

The project's CC0 dedication applies to original project material. It does not replace licenses for dependencies or other content.

- Electron is MIT-licensed and incorporates Chromium, Node.js and other components under their respective licenses. Preserve the `LICENSE`, `LICENSES.chromium.html` and other notices shipped with its runtime when redistributing a portable build.
- TypeScript and Playwright use Apache-2.0; Electron Packager and Node.js type definitions use MIT. Consult installed packages for their exact notices and transitive dependencies.
- FFmpeg is an optional desktop-test tool, not bundled with the app. Its licensing depends on its build options.
- User-provided media, logos and websites retain their own ownership and licensing.

The root `LICENSE` contains the project's CC0 text. Local packaging copies it as `LICENSE.sage-presenter.txt`, preserving Electron's own `LICENSE`.
